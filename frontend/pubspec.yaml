name: arxiv_reader
description: ArXiv Paper Reader with Local LLM Scoring
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.3
  intl: ^0.19.0
  url_launcher: ^6.3.1
  flutter_markdown: ^0.7.3
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
